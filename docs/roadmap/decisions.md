# Roadmap — open decisions log

> Part of [`roadmap/`](README.md). Keep this current — add a row every time
> a decision gets made or a question gets raised, don't let it go stale.

| Question | Status |
|---|---|
| What was the second ("Other") primary use case selected alongside "client presentation tool"? | **Unresolved** — confirm with product owner |
| How should element data survive Revit export (IFC vs glTF `extras`)? | **Decided: IFC**, parsed client-side with `web-ifc` — see [`architecture.md`](architecture.md#element-data-pipeline-ifc) |
| How fine-grained should per-model scale control be? | **Decided: preset dropdown**, standard architectural scales (1:1–1:1000), set once at import — see [`architecture.md`](architecture.md#model-scale-presets) |
| Should motion-sensor walk-through ship in Phase 1 or Phase 4? | **Decided: Phase 4**, alongside the custom native AR build — see [`phases.md`](phases.md#phase-4--native-shell-for-on-site-ar) |
| BaaS/storage provider (Supabase vs Firebase vs custom) | **Reopened 2026-08-07**: a separate Gemini session set up **Cloudflare R2** for object storage directly, without Supabase. Superseded by the reconciliation decision below — see [`history/sessions/2026-08-07-session-03.md`](../history/sessions/2026-08-07-session-03.md) |
| Hosting provider (Vercel vs Netlify vs Cloudflare Pages) | **Decided and live**: Vercel — set up during the Gemini session (`architect-ar.vercel.app`), connected to `main`. Currently serving Gemini's simpler app, not PR #2 — see reconciliation decision below |
| **Reconcile `main` (Gemini's live, simpler paste-a-URL version on Cloudflare R2) with PR #2 (this project's fuller Phase 1: BIM data, scale presets, shareable links, tests)** | **Still formally unresolved**, but new work (the QR code feature) has continued on PR #2's branch since it needs the persistent `/p/:projectId` links that only exist there — see [`history/sessions/2026-08-07-session-03.md`](../history/sessions/2026-08-07-session-03.md). Treat PR #2 as the active direction unless the owner says otherwise; still needs an explicit decision on whether/how to bring Cloudflare R2 in as the storage layer. |
| Unlisted-link vs passcode sharing for Phase 1 | Recommended: unlisted for Phase 1, passcode in Phase 2 |
| New repo vs new directory in this repo for the web app | **Decided and done**: same repo, `web/` directory — scaffolded in Session 1's Phase 0 execution, see [`engineering/folder-structure.md`](../engineering/folder-structure.md) |
| Is client-side IFC parsing fast enough on real mid-range phones? | Not yet tested — profile before Phase 1 ships; fallback is server-side pre-processing (see [`features/element-data-inspection.md`](../features/element-data-inspection.md)) |
| Who's allowed to create a project (upload a model)? | **Known Phase 1 gap, not decided**: currently open to anyone with the public anon key (no architect login exists yet). Must be closed with real auth before any public launch — see `web/supabase/schema.sql` and [`history/sessions/2026-08-06-session-02.md`](../history/sessions/2026-08-06-session-02.md) |
| Does the glTF exporter used actually preserve each element's IFC GlobalId in the node name? | **Confirmed working end-to-end 2026-08-08**: tested against a real Revit-exported IFC file (converted to glTF with IfcOpenShell, an independent open-source exporter) — found it uses a different (expanded-UUID) naming convention than originally assumed, ported the correct compress/expand algorithm, and the resolver now handles both conventions. The owner then live-tested the actual feature on PR #2's `/local` page with these real files and confirmed a real data panel appears on tap. **Still open**: whether the owner's own eventual export pipeline (their actual Revit/SketchUp/Rhino setup) follows either of these two conventions, or a third one. See [`history/sessions/2026-08-08-session-04.md`](../history/sessions/2026-08-08-session-04.md) and [`features/element-data-inspection.md`](../features/element-data-inspection.md) |
| When should the owner's own real project file (not the public Duplex sample) be used to test? | **Deliberately deferred 2026-08-08**: owner's call — the Duplex sample is good enough to keep testing with while more features get built; their own "big model" gets used once the app is more complete, not now. Not a gap, a sequencing choice — revisit before treating any real client link as production-ready. |
| Phase 2 build order (branding, passcode, multiple models, lighting) | **Reordered 2026-08-09**: owner wants to close out all of Phase 2. Branding moved to *last* — owner doesn't have a usable logo file on hand yet. Order is now: multiple models per project (done), passcode-protected links, lighting presets, branding. |
| How does the owner get real Revit textures (not flat colors) into an uploaded model? | **Noted 2026-08-09, to build later**: Revit's own native export commands (IFC, FBX) do not carry real textures/materials by default — confirmed for both, not just an IFC quirk. The free path: Autodesk's **Twinmotion add-in for Revit** exports FBX *with* real materials included at no cost → import that FBX into **Blender** → export GLB with materials on. (Paid alternative: a dedicated Revit→glTF plugin, e.g. SimLab/ProtoTech/Leia/DiRoots, does it in one step.) Not yet tried against this app — verify once attempted. |
| Should the app require a separately-exported GLB at all, or render straight from IFC? | **Decided and built, 2026-08-09**: owner chose to auto-convert IFC to a GLB in-app (with a real progress bar, not a fake spinner) rather than disable AR or require a placeholder GLB. Built as [`features/ifc-only-upload.md`](../features/ifc-only-upload.md) — the GLB upload field is now optional; AR, tap-to-inspect, category hide/show, and levels/rooms navigation all keep working unchanged against the auto-generated GLB. Not yet tested through a real live Supabase upload (no live credentials in this environment). |
| Can "View in AR" ever stay locked to the plane it was placed on? | **Confirmed a hard technical limit, 2026-08-09**: read `<model-viewer>`'s own Scene Viewer intent-building code directly (same file already used to confirm the camera-focus/hidden-categories limit) — the only AR-session parameters it can pass through are `mode`, `disable_occlusion`, `resizable`, and `enable_vertical_placement` (wall-only). Nothing controls post-placement locking or disables Scene Viewer's own tap-to-reposition gesture. Real fix needs Phase 4's custom AR camera (no Scene Viewer handoff at all). |
| Where should "did the client open the link, how long did they look" analytics be visible? | **Decided and built, 2026-08-09**: owner explicitly rejected showing stats inline on a project's own page (visible to anyone with the link) in favor of a separate admin dashboard. Built as [`features/analytics-and-admin-dashboard.md`](../features/analytics-and-admin-dashboard.md) — a new `/admin` route behind a single shared admin passcode (`admin_settings`, completely separate from any per-project passcode), unrelated to and never reachable from a project's own shareable link. |
| **Phase 2 scope** | **Fully built, 2026-08-09**: every item in the original [`phases.md`](phases.md#phase-2--presentation-polish) list is done except a real logo file (branding is currently text-only — see the Revit-textures row above for the unrelated "real textures in the model" question, which is a separate open item). |
