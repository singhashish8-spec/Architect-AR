<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

        <!-- Native AR walkthrough (Phase 4): a separate full-screen
             Activity, not embedded in the WebView, the same shape
             Scene Viewer/Quick Look already use on the web side (see
             docs/features/ar-walkthrough.md). android:exported="false":
             only this app itself ever launches it, via the plugin
             below, never another app via an external Intent. -->
        <activity
            android:name=".ar.ArWalkthroughActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- "optional", not "required": ARCore not being available on a
             device must not block the rest of this app (project
             browsing, quantity takeoff, the existing web-based AR
             handoff) from installing or working; see
             docs/features/ar-walkthrough.md's error-handling section. -->
        <meta-data android:name="com.google.ar.core" android:value="optional" />
    </application>

    <!-- Declares the AR camera feature as used-but-not-required, for the
         same "don't block install on unsupported devices" reason as the
         com.google.ar.core meta-data above. -->
    <uses-feature android:name="android.hardware.camera.ar" android:required="false" />

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Requested at runtime, not just declared here; see
         ArWalkthroughActivity's explicit permission-request flow. -->
    <uses-permission android:name="android.permission.CAMERA" />
</manifest>
